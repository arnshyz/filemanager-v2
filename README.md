
# TeleFile Manager v2

File manager + player + Telegram bot. Dengan autentikasi dasar + UI modern (Tailwind + shadcn-style).

## Jalankan
```bash
cp .env.example .env
npm i
npm run dev
# http://localhost:5173 (web), http://localhost:3000 (api & file)
```
